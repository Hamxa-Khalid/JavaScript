//Create a function that takes four string arguments (noun, adjective, verb, and adverb) and returns a sentence using those words.
function wordBlanksGame(noun, adjective, verb, adverb) {
  return `${adjective} ${noun} ${verb} ${adverb}`;
}

console.log(wordBlanksGame("Hamza", "Love", "doing", "code"));
